{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TradeReport",
  "type": "object",
  "required": [
    "clOrderId",
    "market",
    "securityId",
    "side",
    "qty",
    "price",
    "shareholderId",
    "execId",
    "execQty",
    "execPrice"
  ],
  "properties": {
    "clOrderId": { "type": "string", "maxLength": 16 },
    "market": { "type": "string", "enum": ["XSHG", "XSHE", "BJSE"] },
    "securityId": { "type": "string", "maxLength": 6 },
    "side": { "type": "string", "enum": ["B", "S"] },
    "qty": { "type": "integer" },
    "price": { "type": "number" },
    "shareholderId": { "type": "string", "maxLength": 10 },
    "execId": { "type": "string", "maxLength": 12 },
    "execQty": { "type": "integer" },
    "execPrice": { "type": "number" }
  }
}
